<Window
    x:Class="WpfApp.SliderBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="SliderBar"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <StackPanel>
        <Slider
            x:Name="sld1"
            Margin="10"
            Maximum="100"
            Minimum="0"
            Value="20" />
        <TextBox Text="{Binding Path=Value, ElementName=sld1}" />
        <ProgressBar
            Margin="10"
            Foreground="#FFB02B06"
            Maximum="100"
            Minimum="0"
            Value="{Binding Path=Value, ElementName=sld1}" />
        <ProgressBar
            Margin="10"
            Foreground="Aqua"
            IsIndeterminate="True"
            Maximum="100"
            Minimum="0"
            Value="{Binding Path=Value, ElementName=sld1}" />

    </StackPanel>
</Window>
