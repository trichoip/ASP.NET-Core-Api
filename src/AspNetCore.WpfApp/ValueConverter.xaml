<Window
    x:Class="WpfApp.ValueConverter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:convert="clr-namespace:WpfApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="ValueConverter"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <StackPanel>

        <StackPanel.Resources>
            <convert:AgeConvert x:Key="ageConvert" />
        </StackPanel.Resources>
        <DatePicker x:Name="dpkDate" />
        <TextBlock Text="{Binding ElementName=dpkDate, Path=SelectedDate, Converter={StaticResource ResourceKey=ageConvert}}" />
    </StackPanel>

</Window>
