<Window
    x:Class="WpfApp.XmlWpf"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="XmlWpf"
    Width="800"
    Height="450"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <XmlDataProvider x:Key="ContactList" Source="Contacts.xml" XPath="ContactList/Contact" />
    </Window.Resources>

    <StackPanel>
        <TextBlock Text="{Binding}" TextWrapping="Wrap" />
        <Button Click="Button_Click" Content="click" />
        <ListView Name="lvContacts" Margin="31,14,31,303" ItemsSource="{Binding Source={StaticResource ContactList}}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding XPath=@Id}" Header="Id" />
                    <GridViewColumn Width="100" DisplayMemberBinding="{Binding XPath=ContactName}" Header="Contact Name" />
                    <GridViewColumn Width="200" DisplayMemberBinding="{Binding XPath=Company}" Header="Company" />
                    <GridViewColumn Width="150" DisplayMemberBinding="{Binding XPath=Phone}" Header="Phone" />
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</Window>
