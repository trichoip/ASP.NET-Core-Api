<Window
    x:Class="WpfApp.ContentControls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="clr-namespace:WpfApp.UserControls"
    xmlns:viewmodel="clr-namespace:WpfApp.ViewModel"
    Title="ContentControls"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.DataContext>
        <viewmodel:ItemViewModel />
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodel:HomeVM}">
            <uc:HomeControls />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:MenuVM}">
            <uc:MenuControl />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Button Margin="5" Command="{Binding ShowHomeViewCommand}" Content="Home" />
            <Button Margin="5" Command="{Binding ShowMenuViewCommand}" Content="Menu" />
        </StackPanel>
        <ContentControl Grid.Column="1" Content="{Binding CurrentView}" />
    </Grid>
</Window>
