@*file _ViewStart.cshtml là nơi để bắt đầu chạy layout *@
@*trong file layout bắt buộc phải có =>   @RenderBody() *@
@*đặt tên phải đúng _ViewStart*@
@*MVC => Views/_ViewStart.cshtml*@
@*Razor page => Pages/_ViewStart.cshtml*@
@*layout là file cấu hình bố cục cho toàn views*@
@*tên file layout đặt tùy ý*@
@*nếu page nào có  @RenderBody() thì không thể truy cập get hay post,... chỉ có thể truy cập thông qua Layout như bên dưới*@
@*file layout không được đặt @model vì nó sẽ ghi đè lại hết tất cả @model trong view*@

@{
    //Layout = "/Views/Feature/Index1.cshtml";

    // /Views/Account/render.cshtml
    // /Views/Shared/render.cshtml
    // khi truy cập vào controller nào thì sẽ chạy file render ở trong folder controller đó hoặc trong folder share
    // nêu không có file render ở trong folder controller đó thì sẽ chạy file render ở trong folder share
    // ví dụ: khi truy cập vào controller account thì sẽ chạy file render ở trong folder account
    // khi truy cập vào controller validation thì sẽ chạy file render ở trong folder validation
    // nếu không có file render ở trong folder validation thì sẽ chạy file render ở trong folder share
    // nếu không có file render ở trong folder share thì sẽ báo lỗi
    //Layout = "render";

    // tương tự render
    // khi truy cập vào controller nào thì sẽ chạy file render ở trong folder controller đó hoặc trong folder share
    // khi truy cập vào controller account thì sẽ chạy file render11 ở trong folder account
    // khi truy cập vào controller validation thì sẽ chạy file render11 ở trong folder validation nhưng mà không có file render11 ở trong folder validation
    // nên sẽ chạy file render11 ở trong folder share nhưng ma không có file render11 ở trong folder share thì sẽ báo lỗi
    //Layout = "render11";

    // /Views/Account/_Layout.cshtml
    // /Views/Shared/_Layout.cshtml
    // khi truy cập vào controller nào thì sẽ chạy file _Layout ở trong folder controller đó hoặc trong folder share
    Layout = "_Layout";

    // chọn file layout ở chổ khác folder share và khi truy cập vào controller nào thì vẫn chạy layout dưới
    //Layout = "/Views/Validation/render.cshtml";
    //Layout = "/Views/account/render.cshtml";
}
